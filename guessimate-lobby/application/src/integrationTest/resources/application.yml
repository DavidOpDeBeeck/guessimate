spring:
  datasource:
    name: guessimate
    url: jdbc:tc:postgresql:///guessimate
  liquibase:
    enabled: true
    change-log: classpath:db/changelog/master.xml

smd:
  event-store:
    enabled: true
